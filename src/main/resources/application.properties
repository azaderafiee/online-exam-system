# Server Configuration
server.port=8080
spring.application.name=Online Exam Management System

# H2 Database Configuration
spring.datasource.url=jdbc:postgresql://80.210.19.119:5432/appdb
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=bbbbbb

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=MyVerySecureSecretKeyForJWTTokenGeneration2024!@#$%
jwt.expiration=86400000

# Internationalization
spring.messages.basename=messages
spring.messages.encoding=UTF-8

# Logging
logging.level.com.exammanagement=DEBUG
logging.level.org.springframework.security=DEBUG